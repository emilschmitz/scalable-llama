model = "sprints/Llama-3.2-1B-Instruct"
max_steps = 300

# Training
batch_size = 128
rollouts_per_example = 8

[sampling]
max_tokens = 1024

[[env]]
id = "emilschmitz/curriculum-oversight"
args = { curriculum_type = "steganographic", difficulty_level = 1, remove_llm_verifier = true }
